
// esto es solo un ejemplo, pero podemos crear otra aplicacion mas compleja aqui
function alerta(message) {
    alert(message);
}

// Es una nueva forma de exportar que requiere una nueva forma de imoortar
export {
    alerta
}
